/// This is for news headlines at the top of the home page.
class HeadlineModel {
  final int id;
  final String headlineText;
  final String imageURL;
  final String buttonText;

  const HeadlineModel(this.id, this.headlineText, this.imageURL, this.buttonText);
}